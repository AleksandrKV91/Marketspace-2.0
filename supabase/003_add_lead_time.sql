-- Добавить lead_time_days в fact_china_supply
ALTER TABLE fact_china_supply
  ADD COLUMN IF NOT EXISTS lead_time_days int;

-- Комментарий: лог. плечо в днях, источник — Потребность Китай → Зеленка → колонка «Лог. плечо, дн»
-- Данные только по ~1700 артикулов, остальные NULL
-- При повторных загрузках: upsert обновляет только строки присутствующие в файле (старые значения сохраняются)
